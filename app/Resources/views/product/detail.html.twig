{% extends 'base.html.twig' %}

{% block body %}
    <ol class="breadcrumb">
        <li class="active">{{ product.title }}</li>
    </ol>
    <h1> {{ product.title }}</h1>
    <img src="{{ product.image }}" />
    <h3>Parametre:</h3>
    <ul>
        {% for parameter in parameters %}
            <li>
                {{ parameter.parameter.name }}:
                {% if parameter.parameter.dataType == 'string' %}
                    {{ parameter.valueString }}
                {% elseif parameter.parameter.dataType == 'float' %}
                    {{ parameter.valueFloat }}
                {% elseif parameter.parameter.dataType == 'boolean' %}
                    {% if parameter.valueBoolean %} ano {% else %} nie {% endif %}
                {% endif %}
            </li>
        {% endfor %}
    </ul>
{% endblock %}